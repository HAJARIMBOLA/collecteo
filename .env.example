DB_URL=jdbc:postgresql://<host>:<port>/<database>?sslmode=require
DB_USERNAME=<utilisateur>
DB_PASSWORD=<mot_de_passe>

# OBLIGATOIRE : genere avec `openssl rand -base64 32` (minimum 32 caracteres)
JWT_SECRET=<remplace_par_un_secret_genere_aleatoirement>

# Optionnel — active les logs SQL verbeux en dev si besoin
# JPA_SHOW_SQL=true
# HIBERNATE_SQL_LOG_LEVEL=debug